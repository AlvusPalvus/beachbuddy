import React from "react";
type Beach = {
    name: string;
};

type Props = {
    beach: Beach;
};

const BeachCard = (props: Props) => {
    return <div>BeachCard</div>;
};

export default BeachCard;
